
  # Website builder

  This is a code bundle for Website builder. The original project is available at https://www.figma.com/design/N76NJtSmgqz2gQ0sJQMRLL/Website-builder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  